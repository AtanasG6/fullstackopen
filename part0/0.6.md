# 0.6: New note in Single page app diagram

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: Потребителят пише бележка и натиска Save
    Note right of browser: onsubmit извиква e.preventDefault, така че няма стандартно изпращане на формата
    Note right of browser: Бележката се добавя в локалния масив и се рендира веднага с DOM API, без презареждане

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note right of browser: Тяло JSON с content и date, Content-Type application/json
    activate server
    Note left of server: Сървърът добавя бележката в масива notes
    server-->>browser: HTTP 201 created
    deactivate server

    Note right of browser: Няма redirect и няма повече заявки, страницата не се презарежда
```